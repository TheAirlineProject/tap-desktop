<Page x:Class="TheAirlineV2.GraphicsModel.PageModel.PageAirportsModel.PanelAirportsModel.PageAirportsStatistics"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="PageAirportStatistics">
    <Page.Resources>
        <DataTemplate x:Key="AirportItem">
            <WrapPanel>
                <ContentControl ContentTemplate="{DynamicResource CountryFlagItem}" Content="{Binding Airport.Profile.Country}"></ContentControl>
                <TextBlock Width="250" VerticalAlignment="Bottom">
                    <Hyperlink Tag="{Binding Airport}" Click="LnkAirport_Click">
                        <Run Text="{Binding Airport.Profile.Name}"></Run>
                    </Hyperlink>
                </TextBlock>
                <TextBlock Text="{Binding Total}" VerticalAlignment="Bottom" Width="50" TextAlignment="Right"></TextBlock>
              </WrapPanel>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        
    </Grid>
</Page>
