<Page x:Class="TheAirline.GraphicsModel.PageModel.PageAirportModel.PanelAirportModel.PageAirportTraffic"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="PageAirportTraffic">
    <Page.Resources>
        <DataTemplate x:Key="DestinationItem">
            <WrapPanel>
                <ContentControl ContentTemplate="{DynamicResource AirportCountryLink}" Content="{Binding Key}" VerticalAlignment="Bottom" Width="200"></ContentControl>
                <TextBlock Width="75" TextAlignment="Right" Text="{Binding Value}" VerticalAlignment="Bottom"></TextBlock>
            </WrapPanel>
        </DataTemplate>
    </Page.Resources>
        <Grid>
        
    </Grid>
</Page>
